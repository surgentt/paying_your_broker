
var retirementAge = 65;
var growth_rate = 8;
var vanguard_ratio = 0.17;

var pv = <%= @present_value.to_i %>
var pmt = <%= @pmt.to_i %>
var age = <%= @age.to_i %>
var exp = <%= @exp_ratio.to_f %>

var port1 = new PortfolioValue(pv, pmt, age, exp);
var port2 = new PortfolioValue(pv, pmt, age, vanguard_ratio);
var gift = port2.fv - port1.fv
var ripoff = "$" + (gift).formatMoney(0);
$("#from-controller p").html(ripoff)